---

vault_version:          1.7.3
vault_home:             /srv/vault
vault_init_file:        "{{ vault_home }}/config/.vault_init"
vault_unseal_file:      "{{ vault_home }}/config/.vault_unseal"
vault_auth_jwt_file:    "{{ vault_home }}/config/.vault_auth_jwt"
vault_show_unseal_keys: no
vault_show_root_token:  no

...